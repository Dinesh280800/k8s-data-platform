# =============================================================================
# PROMETHEUS + GRAFANA - Helm Values (kube-prometheus-stack)
# =============================================================================
# This values file configures the kube-prometheus-stack Helm chart which deploys:
# - Prometheus (metrics collection and storage)
# - Grafana (visualization dashboards)
# - Alertmanager (alert routing and notification)
# - Node Exporter (host metrics)
# - kube-state-metrics (Kubernetes object metrics)
#
# INSTALL COMMAND:
#   helm repo add prometheus-community https://prometheus-community.github.io/helm-charts
#   helm repo update
#   helm install monitoring prometheus-community/kube-prometheus-stack \
#     --namespace monitoring \
#     --values monitoring/prometheus/values.yaml \
#     --wait
#
# KEY SECTIONS:
# - prometheus: Server config, retention, storage
# - grafana: Dashboards, datasources, admin credentials
# - alertmanager: Alert routing and receivers
# - nodeExporter: Host-level metrics
# - kubeStateMetrics: Kubernetes object state metrics
#
# CUSTOMIZATION:
# - Adjust retention/storage for your disk capacity
# - Add custom scrape configs for your services
# - Configure alertmanager receivers (Slack, PagerDuty, email)
# =============================================================================

# --- Global Settings ---
global:
  scrape_interval: 15s                  # How often Prometheus scrapes targets
  evaluation_interval: 15s              # How often to evaluate alert rules

# --- Prometheus Server ---
prometheus:
  enabled: true

  prometheusSpec:
    # Retention: how long to keep metrics data
    retention: 3d                        # Keep 3 days of data for a small local cluster
    retentionSize: "2GB"                 # Smaller max storage size

    # Resource limits for Prometheus server
    resources:
      requests:
        cpu: 100m
        memory: 256Mi
      limits:
        cpu: 500m
        memory: 768Mi

    # Storage configuration
    storageSpec:
      volumeClaimTemplate:
        spec:
          accessModes: ["ReadWriteOnce"]
          resources:
            requests:
              storage: 2Gi              # Smaller PVC size for local development

    # Service Monitor selector - scrape all ServiceMonitors in all namespaces
    serviceMonitorSelectorNilUsesHelmValues: false
    serviceMonitorSelector: {}
    serviceMonitorNamespaceSelector: {}

    # Pod Monitor selector
    podMonitorSelectorNilUsesHelmValues: false
    podMonitorSelector: {}
    podMonitorNamespaceSelector: {}

    # Additional scrape configs for pods with prometheus.io annotations
    additionalScrapeConfigs:
      - job_name: 'kubernetes-pods'
        kubernetes_sd_configs:
          - role: pod
        relabel_configs:
          - source_labels: [__meta_kubernetes_pod_annotation_prometheus_io_scrape]
            action: keep
            regex: true
          - source_labels: [__meta_kubernetes_pod_annotation_prometheus_io_path]
            action: replace
            target_label: __metrics_path__
            regex: (.+)
          - source_labels: [__address__, __meta_kubernetes_pod_annotation_prometheus_io_port]
            action: replace
            regex: ([^:]+)(?::\d+)?;(\d+)
            replacement: $1:$2
            target_label: __address__
          # Drop non-metrics ports (Loki memberlist 7946, gRPC 9095)
          - source_labels: [__address__]
            action: drop
            regex: .*:(7946|9095)
          - action: labelmap
            regex: __meta_kubernetes_pod_label_(.+)
          - source_labels: [__meta_kubernetes_namespace]
            action: replace
            target_label: kubernetes_namespace
          - source_labels: [__meta_kubernetes_pod_name]
            action: replace
            target_label: kubernetes_pod_name

# --- Grafana ---
grafana:
  enabled: true

  # Disable the chart's auto-generated default Prometheus datasource
  # We define our own below to avoid conflict with loki-stack's datasource
  defaultDatasourceEnabled: false

  # Admin credentials (change in production!)
  adminUser: admin
  adminPassword: admin-password         # CHANGE THIS in production

  # Resource limits
  resources:
    requests:
      cpu: 50m
      memory: 64Mi
    limits:
      cpu: 200m
      memory: 256Mi

  # Persistence for dashboards and settings
  persistence:
    enabled: false

  # Datasources — Loki and Alertmanager only (Prometheus is auto-provisioned by the chart)
  additionalDataSources:
    - name: Loki
      type: loki
      url: http://loki.monitoring.svc.cluster.local:3100
      access: proxy
      isDefault: false
    - name: Alertmanager
      type: alertmanager
      url: http://monitoring-kube-prometheus-alertmanager.monitoring.svc.cluster.local:9093
      access: proxy
      isDefault: false
      jsonData:
        implementation: prometheus

  # Service type for access
  service:
    type: NodePort
    nodePort: 30000                     # Access Grafana at localhost:30000

  # Sidecar for auto-loading dashboards from ConfigMaps
  sidecar:
    dashboards:
      enabled: true
      searchNamespace: ALL

# --- Alertmanager ---
alertmanager:
  enabled: true

  alertmanagerSpec:
    resources:
      requests:
        cpu: 25m
        memory: 32Mi
      limits:
        cpu: 100m
        memory: 128Mi

  # Alertmanager configuration (routing + receivers)
  config:
    global:
      resolve_timeout: 5m               # Mark alert resolved after 5m of silence
      smtp_smarthost: 'smtp.gmail.com:587'
      smtp_from: 'your-email@gmail.com'
      smtp_auth_username: 'your-email@gmail.com'
      smtp_auth_password: 'your-app-password'   # Use Gmail App Password (not your login password)
      smtp_require_tls: true

    # Route tree: how alerts are grouped and routed
    route:
      group_by: ['alertname', 'namespace', 'job']
      group_wait: 30s                   # Wait before sending first notification
      group_interval: 5m                # Wait before sending updates for a group
      repeat_interval: 4h               # Re-send if alert still firing after 4h
      receiver: 'email-receiver'        # Default receiver
      routes:
        - match:
            severity: critical
          receiver: 'email-critical'
          group_wait: 10s               # Send critical alerts faster
        - match:
            severity: warning
          receiver: 'email-receiver'

    # Receivers: where alerts are sent
    receivers:
      - name: 'email-receiver'
        email_configs:
          - to: 'your-email@gmail.com'
            send_resolved: true
            headers:
              Subject: '[WARNING] {{ .GroupLabels.alertname }} in {{ .GroupLabels.namespace }}'

      - name: 'email-critical'
        email_configs:
          - to: 'your-email@gmail.com'
            send_resolved: true
            headers:
              Subject: '[CRITICAL] {{ .GroupLabels.alertname }} in {{ .GroupLabels.namespace }}'

    # Inhibition rules: suppress less severe alerts when critical is firing
    inhibit_rules:
      - source_match:
          severity: 'critical'
        target_match:
          severity: 'warning'
        equal: ['alertname', 'namespace']

# --- Node Exporter ---
nodeExporter:
  enabled: true                         # Collects host-level metrics (CPU, disk, network)

# --- kube-state-metrics ---
kubeStateMetrics:
  enabled: true                         # Collects Kubernetes object state metrics

# --- Disable control-plane monitors that don't work in kind ---
kubeControllerManager:
  enabled: false

kubeScheduler:
  enabled: false

kubeEtcd:
  enabled: false

kubeProxy:
  enabled: false

# --- Prometheus Operator ---
prometheusOperator:
  enabled: true
  resources:
    requests:
      cpu: 50m
      memory: 64Mi
    limits:
      cpu: 200m
      memory: 256Mi
